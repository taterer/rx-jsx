export enum Route {
  home = '',
  calc = 'calc',
  draw = 'draw',
  babylon = 'babylon',
}

export const RouteRegExp = {
  '': /^$/,
  home: /^$/,
  calc: /^calc$/,
  draw: /^draw$/,
  babylon: /^babylon$/,
}
